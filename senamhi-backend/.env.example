# Database Configuration
# Local usage
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=senamhi_user
DB_PASSWORD=senamhi_password
DB_NAME=senamhi_fenologic

# Cloud Deployment (e.g. Render/Railway/Supabase)
# If DATABASE_URL is provided, individual DB_* vars are ignored
DATABASE_URL=postgresql://user:password@host:port/database

# App Configuration
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173

# Security
JWT_SECRET=cambiame-por-una-llave-segura-en-produccion
